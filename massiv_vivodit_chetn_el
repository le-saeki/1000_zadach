#include <iostream>

using namespace std;
 
int f(int s)
{
    int Arr[5] {8, 3, 4, 9, 12};
    for (int i = 0; i < s; ++i)
    {
    	if(Arr[i]%2==0)
    	{cout<<Arr[i];}
    	cout<<" ";}
}
int main()
{
	cout<<f(5);
  return 0;
}
