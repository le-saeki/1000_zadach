#include <iostream>
using namespace std;

int find_if(int*arr, int last, int first, int value)
{
	for(;first<last;++first)
{
	if(arr[first]==value)
{return first;}}
return -1;
}

int main() {
	int array [5]{1,2,6,5,7};
	std::cout<<find_if(array,5,0,5)<<std::endl;
	return 0;
}
